{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的核心能力。没有这个文件，Tauri 2 会拒掉 plugin:event|listen —— 自定义命令照常能 invoke，但后端 emit 的事件一个都到不了前端，界面会永远停在「生成中」而后台其实早就写完盘了。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:event:allow-emit-to"
  ]
}
